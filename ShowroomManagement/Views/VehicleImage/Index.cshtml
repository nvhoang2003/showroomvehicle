@model IEnumerable<ShowroomManagement.Models.vehicle_image>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.image_url)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vehicle_data.model_number)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.image_url)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.vehicle_data.model_number)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.image_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.image_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.image_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
